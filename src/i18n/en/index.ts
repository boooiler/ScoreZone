import root from '@/root/i18n/en'
import shared from '@/shared/i18n/en'
import { common } from "./common"
import { errors } from "./errors"

const englishTranslations = {
  root,
  shared,
  common,
  errors
}

export default englishTranslations