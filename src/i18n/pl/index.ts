import root from '@/root/i18n/pl'
import shared from '@/shared/i18n/pl'
import { common } from "./common"
import { errors } from "./errors"

const polishTranslations = {
  root,
  shared,
  common,
  errors
}

export default polishTranslations