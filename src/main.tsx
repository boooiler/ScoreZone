import React from 'react'
import ReactDOM from 'react-dom/client'
import ScoreZoneApp from './root/App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ScoreZoneApp />
  </React.StrictMode>
)
